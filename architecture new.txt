You have full control. Do NOT break translations (EN + MK already exist). Keep all current URLs and browse behavior. Server is source of truth. No heavy client filtering.

TASK: Refactor /browse filters UI to be clean, compact, premium and NOT duplicated. Do NOT change business logic, only UI structure and state wiring.

First do:
1) /run git status
2) Identify where the filters UI is rendered (likely src/components/browse-filters.tsx + src/app/browse/page.tsx).
3) List max 6 files you will touch, then implement.

Requirements (UI/UX):
A) Top bar (always visible) should contain ONLY:
- Search input (prominent)
- Sort segmented control (compact)
- A “Filters” button with active count badge (e.g. Filters (3))
- Active filter chips row + “Clear all”

B) All other filters must live inside a Sheet/Drawer (right side on desktop, bottom sheet on mobile):
- Category, Subcategory, City, Condition
- Price min/max
- If category is Cars: Car-specific fields (make/model/yearFrom/yearTo/fuel/transmission/kmFrom/kmTo)
- No duplicate fields. If Cars-specific fields exist, do not also show the same ones in a generic “category filters” section.

C) Visual polish:
- Use clear section headers inside drawer
- Consistent spacing, fewer borders, compact controls
- Default collapsed advanced section if needed (e.g. “More filters” accordion)

D) Behavior:
- URL params remain exactly the same; removing a chip updates URL
- “Clear all” resets all filters (keeps q empty too)
- “Reset filters” in drawer does same
- Keep realtime updates if currently realtime; do not add heavy client filtering

Translations:
- Add EN/MK labels for: Filters, Clear all, Reset, More filters, Price, Vehicle details (if new strings)

After:
- /run pnpm typecheck (or lint)
- Show git diff summary
- Confirm the duplicate Brand/Model/Year/Km fields are removed (only one source of truth).