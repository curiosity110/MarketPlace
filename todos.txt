
------------------------------------------------------------------------------------------------------------------------------







---------------------------------------------------------------------------------------------------------


CHANGES TO SEE


You have full control. Do NOT break translations (EN + MK already exist). Keep business logic and auth.
Goal: Make Home + Browse dramatically faster by removing heavy Prisma includes and using a lightweight ListingCard DTO.

Constraints:
- Do NOT change UI behavior or URLs.
- Keep prisma circuit breaker logic.
- Server is source of truth.

1) Create src/lib/listing-card-select.ts exporting listingCardSelect with ONLY:
id, title, priceCents, currency, createdAt, status, condition,
city { id, name },
category { id, name, slug, parent { id, name, slug } },
images { id, url } take:1 orderBy: (order asc if exists else createdAt asc)
NO fieldTemplates, NO fieldValues, NO seller email, NO description.

2) Update ListingCard to accept this DTO shape (minimal changes).

3) Update Home and Browse listing.findMany calls to use select: listingCardSelect.

4) Cache categories + cities lists with revalidate 300s (safe). Do not heavily cache listing results.

Verify build passes and pages render correctly.




You must verify the code changes by executing the following commands in the repository:
Never assume types. If unsure, inspect the repository files before writing code.

pnpm install
pnpm prisma generate
pnpm prisma validate
pnpm lint
pnpm tsc --noEmit
pnpm build

The task is NOT complete unless ALL commands exit with code 0.

If something fails, fix the code and rerun until success.

Return:
- command outputs
- modified files
- explanation of what was changed







---------------------------------------------------------------------------------------------------------


PHONE CHANGES TO SEEE



You have full control. Do NOT break translations (EN + MK already exist). Keep server browse query logic.
Goal: Make /browse filters OCD-clean on mobile with a Filter Sheet + Apply button, and add an Active Filters chips row.

Constraints:
- Desktop stays real-time (debounced 250–400ms).
- Mobile uses Sheet state; only updates URL on Apply.
- URL stays shareable.
- No heavy client filtering.

1) Create src/components/browse/mobile-filter-sheet.tsx ("use client"):
- Trigger button “Filters”
- Sheet with sections (Accordion): Category, City, Price, Condition, Sort
- Sticky footer: Clear + Apply
- Maintains local draft state on mobile.

2) Update BrowseFilters to support two modes:
- mode="desktop": current live behavior
- mode="mobile": controlled values + onApply callback

3) Add Active Filters Chips row above results:
- Chips for q, category, city, price, condition, sort
- Each chip removable => updates URL and resets page=1
- Clear all button

4) On mobile layout:
- Show only Search bar + Filters button + chips
- Hide the full filter grid.

Add EN+MK strings: Filters, Apply, Clear, Active filters, Favorites only (optional).



You must verify the code changes by executing the following commands in the repository:
Never assume types. If unsure, inspect the repository files before writing code.

pnpm install
pnpm prisma generate
pnpm prisma validate
pnpm lint
pnpm tsc --noEmit
pnpm build

The task is NOT complete unless ALL commands exit with code 0.

If something fails, fix the code and rerun until success.

Return:
- command outputs
- modified files
- explanation of what was changed




---------------------------------------------------------------------------------------------------------
by photo info feauture

You have full control. Do NOT break translations (EN + MK already exist). Do NOT change payment logic.
Goal: Redesign CreateListingPopout into a clean 3-step wizard + add optional “Smart Fill from Photos” suggestions.

Constraints:
- Keep existing submit action and Prisma logic.
- Keep category templates logic.
- Must be mobile-first and feel premium.

1) Convert CreateListingPopout UI into steps:
Step 1 Basics: title, category/sub, price+currency, city, condition
Step 2 Photos: upload + preview + optional “Smart Fill” button
Step 3 Details: phone + category fields + review + publish

2) Smart Fill:
- Add a server action analyzeListingPhotos(files) that returns suggestions:
  { suggestedTitle, suggestedCategorySlug?, suggestedBrand?, suggestedModel?, suggestedYear?, suggestedCondition?, suggestedDescription }
- IMPORTANT: this returns suggestions only (no auto-save).
- UI shows suggestions with “Apply” buttons per field.

3) Make the right column (category/location card) disappear on mobile; wizard becomes single column.

4) Improve spacing, alignment, typography, and remove “form dump” feeling.

Add EN+MK strings for step labels and smart fill UI.




---------------------------------------------------------------------------------------------------------








---------------------------------------------------------------------------------------------------------







---------------------------------------------------------------------------------------------------------